// const express = require('express')

// const app = express()

// app.use(express.json())

// app.use("/api/movieAPI", require("./movieAPI"));
// app.use("/api/reservation", require("./reservations"));
// app.use("/api/showtimes", require("./showtimes"));
// app.use("/api/theater", require("./theater"));
// app.use("/api/users", require("./users"));

// app.listen(port, () => {
//     console.log(`Server running on port: ${port}`);
// });
